spring:
  application:
    name: User-service
  datasource:
    url: ${SPRING_DATASOURCE_URL_USERS}
    username: ${SPRING_DATASOURCE_USERNAME_USERS}
    password: ${SPRING_DATASOURCE_PASSWORD_USERS}
    driver-class-name: ${SPRING_DATASOURCE_DRIVER_CLASS_NAME_USERS}
